package algoritmsPractice;

import java.lang.reflect.Array;
import java.util.*;

public class CountLetter {
    public static void main(String[] args) {


      // 1-Task count how many 't' we have use for loop
      // 2-Task  count each letter using map, forLoop, If else
      // 3-Task count each letter using map.getDefaultContent









      // 1-Task count how manu 't' we have use forloop
//      int count = 0;
//      for (int i = 0; i < word.length(); i++) {
//        if (word.charAt(i)== targetchar){
//          count++;
//        }
//      }
//      System.out.println(count);

      //======== Task 2  count each letter using map, forLoop, If else
//      String word1 = "I use Automation Cccccccqqqucumber";
//      String word = word1.toLowerCase().replace(" ", "");
//
//      Map<Character,Integer> map = new HashMap<>();
//      for (int i = 0; i < word.length(); i++) {
//        Character current = word.charAt(i);
//        Integer count = map.get(current);
//
//        if (map.containsKey(current)){
//          map.put(current, count+1);
//        } else {
//          map.put(current,1);
//        }
//      }
//      System.out.println(map);



//      // 3-Task count each letter using map
//      Map<Character,Integer> map= new HashMap<>();
//      for (int i = 0; i < word.length(); i++) {
//        char current = word.charAt(i);
//        int count1 = map.getOrDefault(current,0);   // Get the current count or use 0 if the letter is not yet in the map
//        map.put(current,count1+1);    // Put the letter back into the map with the updated count
//      }
//
//      // most short one
//      for (char c: word.toCharArray()) {
//        map.put(c, map.getOrDefault(c, 0) + 1);
//      }





    }
}
