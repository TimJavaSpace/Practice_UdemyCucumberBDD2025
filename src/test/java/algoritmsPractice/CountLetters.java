package algoritmsPractice;

import org.openqa.selenium.By;
import org.openqa.selenium.Keys;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.WebElement;
import org.openqa.selenium.chrome.ChromeDriver;
import org.openqa.selenium.devtools.v129.fetch.model.AuthChallengeResponse;
import org.openqa.selenium.devtools.v129.network.model.Response;

import java.util.Arrays;
import java.util.HashMap;
import java.util.Map;

public class CountLetters {
    public static void main(String[] args) throws InterruptedException {



    }

//        String word1 = "I use Cucumber for Automation";
//        String word = word1.replace(" ", "").toLowerCase();
//
//        char[] target = word.toLowerCase().toCharArray();
//        Map<Character, Integer> map = new HashMap<>();
//
//        for (int i = 0; i < word.length(); i++) {
//
//            Character current = word.charAt(i);
//
//
//            if (map.containsKey(current)) {
//                map.put(current, map.get(current) + 1);
//            }
//            else {
//                map.put(current,1);
////            Character current = word.charAt(i);
////            int count = map.getOrDefault(current,0);
////            map.put(current,count+1);
//            }
//        }
//        System.out.println(map);
//    }
}
